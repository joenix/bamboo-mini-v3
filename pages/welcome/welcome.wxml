<view class="welcome-page-cover" wx:if="{{visible}}" style="background-image: url({{bgImage}})">
  <view class="welcome-logo"></view>
  <view class="welcome-btn-entry" bind:tap="start"></view>
</view>